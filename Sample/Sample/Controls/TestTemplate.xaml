<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Sample.ViewModels"
             x:Class="Sample.Controls.TestTemplate"
             x:DataType="viewmodels:SomeModel">
    <Grid HeightRequest="35" BackgroundColor="Red">
        <Label VerticalTextAlignment="Center" Text="{Binding Value}" TextColor="White"/>

        <Grid x:Name="HighlightContainer" Opacity="0" BackgroundColor="Blue"/>
    </Grid>
</ContentView>